# 🚀 Пошаговий план міграції на Ukraine.com.ua

## 📋 Перед початком

### Що потрібно:
- ✅ Аккаунт на Railway (для експорту даних)
- ✅ Аккаунт на ukraine.com.ua (замовити VPS)
- ✅ Доступ до SSH
- ✅ Домен (опціонально, для HTTPS)

### Оцінка часу:
- Замовлення VPS: 10-30 хвилин
- Налаштування сервера: 30-60 хвилин
- Міграція даних: 15-30 хвилин
- Тестування: 15-30 хвилин
- **Всього: 1.5-2.5 години**

---

## Крок 1: Замовлення VPS на ukraine.com.ua

### 1.1. Відкрити сайт
Перейдіть на: https://www.ukraine.com.ua/ru

### 1.2. Вибрати тариф VPS
Рекомендовані тарифи:
- **⚠️ Мінімальний (НЕ рекомендовано)**: 2GB RAM, 20GB SSD, 1 CPU (~264 грн/міс)
  - ❌ Буде недостатньо для Whisper моделей
  - ❌ Можна використовувати тільки Tiny (і то з проблемами)
- **✅ Рекомендований (мінімум)**: 4GB RAM, 40GB SSD, 1 CPU (~500-600 грн/міс)
  - ✅ Комфортна робота з Base моделлю
  - ✅ Можливість використання Small
  - ✅ Місце для AI моделей
- **✅ Для побутового використання**: 6GB RAM, 50GB SSD, 2 CPU (**~798 ₴/міс**)
  - ✅ Комфортна робота з Base та Small моделями
  - ✅ 2 ядра CPU для швидкої обробки
  - ✅ 50 GB диску для файлів
  - ✅ 5 TB трафіку
  - ⚠️ Medium модель можлива, але обмежено
- **⭐ Оптимальний**: 8GB RAM, 70GB NVMe, 4 CPU (**23.84 €/міс** ≈ 950-1000 грн/міс)
  - ✅ Можливість використання Medium моделі
  - ✅ Комфортна робота з Small
  - ✅ Великий резерв для масштабування
  - ✅ 4 ядра CPU для швидкої обробки
  - ✅ 10 TB трафіку

**⚠️ ВАЖЛИВО: VPS 2GB буде недостатньо!** Див. `VPS_MEMORY_REQUIREMENTS.md` для деталей.

**Для WhisperFlow рекомендую мінімум 4GB RAM** (Whisper моделі потребують багато пам'яті).

**Для побутового використання оптимально VPS 6GB** - хороший баланс між ціною та продуктивністю.

### 1.3. Вибрати ОС
- **Ubuntu 22.04 LTS** (рекомендовано)
- Або **Debian 11/12**

### 1.4. Замовити та оплатити
Після замовлення ви отримаєте:
- IP адресу сервера
- Логін (зазвичай `root`)
- Пароль або SSH ключ

---

## Крок 2: Експорт даних з Railway

### 2.1. Підключитися до Railway PostgreSQL

**Варіант A: Через Railway CLI**
```bash
# Встановити Railway CLI (якщо ще не встановлено)
npm i -g @railway/cli

# Увійти
railway login

# Підключитися до проекту
railway link

# Отримати DATABASE_URL
railway variables
```

**Варіант B: Через Railway Dashboard**
1. Відкрийте ваш проект на Railway
2. Перейдіть в **PostgreSQL** сервіс
3. Відкрийте **Connect** → **Postgres Connection URL**
4. Скопіюйте `DATABASE_URL`

### 2.2. Експортувати дані

Використайте скрипт `export_railway_data.py` (буде створено):

```bash
# Встановити залежності
pip install psycopg2-binary

# Запустити експорт
python scripts/export_railway_data.py
```

Або вручну через pg_dump:
```bash
# Встановити PostgreSQL клієнт
# Windows: завантажити з postgresql.org
# Linux/Mac: sudo apt install postgresql-client

# Експортувати базу
pg_dump "postgresql://user:pass@host:port/dbname" > backup.sql
```

### 2.3. Експортувати файли (опціонально)

Якщо потрібно зберегти завантажені аудіо файли:
```bash
# Через Railway CLI
railway run bash
# Потім скопіювати файли з /app/backend/storage
```

---

## Крок 3: Підготовка VPS сервера

### 3.1. Підключення до сервера

```bash
ssh root@your-server-ip
# або
ssh root@your-domain.com.ua
```

### 3.2. Оновлення системи

```bash
apt update && apt upgrade -y
```

### 3.3. Встановлення Docker

```bash
# Встановити Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Встановити Docker Compose
apt install docker-compose-plugin -y

# Перевірити
docker --version
docker compose version
```

### 3.4. Налаштування firewall

```bash
# Встановити UFW
apt install ufw -y

# Дозволити SSH, HTTP, HTTPS
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp

# Увімкнути
ufw enable
```

---

## Крок 4: Розгортання проекту

### 4.1. Клонування репозиторію

```bash
cd /opt
git clone https://github.com/your-username/whisperflow.git
cd whisperflow
```

### 4.2. Створення .env файлу

```bash
cp env.example .env
nano .env
```

Налаштуйте:
```env
# Для PostgreSQL з docker-compose
DATABASE_URL=postgresql://whisperflow_user:your_secure_password@db:5432/whisperflow
POSTGRES_PASSWORD=your_secure_password

# Згенеруйте безпечний SECRET_KEY
SECRET_KEY=your-random-secret-key-here-min-32-chars
PORT=8000
ENVIRONMENT=production
```

**Згенерувати SECRET_KEY:**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 4.3. Запуск через Docker Compose

```bash
docker compose up -d --build
```

### 4.4. Перевірка статусу

```bash
# Перевірити контейнери
docker compose ps

# Перевірити логи
docker compose logs -f app
```

---

## Крок 5: Імпорт даних

### 5.1. Імпорт бази даних

Використайте скрипт `import_data.py`:

```bash
python scripts/import_data.py
```

Або вручну:
```bash
# Скопіювати backup.sql на сервер
scp backup.sql root@your-server-ip:/tmp/

# Імпортувати
docker compose exec -T db psql -U whisperflow_user -d whisperflow < /tmp/backup.sql
```

### 5.2. Перевірка даних

```bash
# Підключитися до бази
docker compose exec db psql -U whisperflow_user -d whisperflow

# Перевірити таблиці
\dt

# Перевірити користувачів
SELECT COUNT(*) FROM users;

# Вийти
\q
```

---

## Крок 6: Налаштування Nginx та HTTPS

### 6.1. Встановлення Nginx

```bash
apt install nginx -y
```

### 6.2. Створення конфігурації

```bash
nano /etc/nginx/sites-available/whisperflow
```

Вставте:
```nginx
server {
    listen 80;
    server_name your-domain.com.ua;  # або IP

    client_max_body_size 500M;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }
}
```

### 6.3. Активувати конфігурацію

```bash
ln -s /etc/nginx/sites-available/whisperflow /etc/nginx/sites-enabled/
rm /etc/nginx/sites-enabled/default
nginx -t
systemctl reload nginx
```

### 6.4. Отримати SSL сертифікат

```bash
# Встановити Certbot
apt install certbot python3-certbot-nginx -y

# Отримати сертифікат (якщо є домен)
certbot --nginx -d your-domain.com.ua

# Certbot автоматично:
# - Отримає сертифікат
# - Налаштує HTTPS
# - Налаштує автоматичне оновлення
```

---

## Крок 7: Налаштування бекапів

### 7.1. Створити скрипт бекапу

```bash
nano /opt/backup-whisperflow.sh
```

Вміст (див. `scripts/backup.sh`):
```bash
#!/bin/bash
BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

# Бекап бази
docker compose -f /opt/whisperflow/docker-compose.yml exec -T db pg_dump -U whisperflow_user whisperflow > $BACKUP_DIR/db_$DATE.sql

# Бекап файлів
tar -czf $BACKUP_DIR/storage_$DATE.tar.gz -C /opt/whisperflow/backend storage/

# Видалити старі бекапи (старіші за 7 днів)
find $BACKUP_DIR -type f -mtime +7 -delete
```

```bash
chmod +x /opt/backup-whisperflow.sh
```

### 7.2. Налаштувати cron

```bash
crontab -e
```

Додати:
```
0 3 * * * /opt/backup-whisperflow.sh
```

---

## Крок 8: Тестування

### 8.1. Перевірка доступності

```bash
# Перевірити, що сервер відповідає
curl http://localhost:8000

# Або через браузер
# http://your-server-ip:8000
# або https://your-domain.com.ua
```

### 8.2. Перевірка функціональності

- [ ] Вхід в систему
- [ ] Завантаження аудіо файлу
- [ ] Транскрипція працює
- [ ] AI аналіз працює
- [ ] Експорт працює
- [ ] Всі дані на місці

### 8.3. Перевірка логів

```bash
docker compose logs app | tail -50
```

---

## Крок 9: Перемикання DNS (якщо є домен)

### 9.1. Оновити DNS записи

В панелі управління доменом:
- **A запис**: `@` → IP сервера
- **A запис**: `www` → IP сервера

### 9.2. Дочекатися поширення DNS

Зазвичай 5-60 хвилин, іноді до 24 годин.

### 9.3. Перевірити

```bash
nslookup your-domain.com.ua
```

---

## Крок 10: Фінальна перевірка

### 10.1. Моніторинг

```bash
# Використання ресурсів
docker stats

# Використання диску
df -h

# Використання пам'яті
free -h
```

### 10.2. Безпека

- [ ] Firewall налаштовано
- [ ] SSL сертифікат працює
- [ ] Паролі змінені на безпечні
- [ ] SSH ключі налаштовані

---

## Крок 11: Вимкнення Railway (опціонально)

**Увага!** Вимкніть Railway тільки після повного тестування нового сервера.

1. Відкрийте Railway Dashboard
2. Перейдіть в Settings проекту
3. Натисніть "Delete Project" або просто зупиніть сервіси
4. Це заощадить кошти

---

## 🔧 Вирішення проблем

### Проблема: Контейнер не запускається

```bash
# Перевірити логи
docker compose logs app

# Перевірити конфігурацію
docker compose config

# Перезапустити
docker compose restart
```

### Проблема: База даних не підключається

```bash
# Перевірити статус
docker compose ps db

# Перевірити логи
docker compose logs db

# Перевірити .env файл
cat .env | grep DATABASE_URL
```

### Проблема: Nginx не працює

```bash
# Перевірити конфігурацію
nginx -t

# Перезапустити
systemctl restart nginx

# Перевірити статус
systemctl status nginx
```

### Проблема: SSL не працює

```bash
# Перевірити сертифікат
certbot certificates

# Оновити вручну
certbot renew
```

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте логи: `docker compose logs`
2. Перевірте документацію: `DEPLOY_VPS.md`
3. Зверніться до техпідтримки ukraine.com.ua
4. Перевірте GitHub Issues

---

## ✅ Чеклист міграції

- [ ] Замовлено VPS на ukraine.com.ua
- [ ] Отримано доступ до сервера
- [ ] Експортовано дані з Railway
- [ ] Встановлено Docker на VPS
- [ ] Розгорнуто проект
- [ ] Імпортовано дані
- [ ] Налаштовано Nginx
- [ ] Отримано SSL сертифікат
- [ ] Налаштовано бекапи
- [ ] Протестовано всі функції
- [ ] Оновлено DNS (якщо є домен)
- [ ] Вимкнено Railway (після тестування)

---

## 🎉 Готово!

Після виконання всіх кроків ваш сайт буде працювати на ukraine.com.ua з HTTPS!

**Наступні кроки:**
- Налаштувати моніторинг (опціонально)
- Налаштувати автоматичні оновлення
- Регулярно перевіряти бекапи

