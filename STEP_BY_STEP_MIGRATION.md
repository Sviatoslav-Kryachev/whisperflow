# 🚀 Пошаговая миграция WhisperFlow на VPS с PostgreSQL

## 📋 Информация о вашем VPS

- **Домен**: app-toolbox.space
- **VPS**: 6GB RAM, 2 CPU, 50GB SSD
- **ОС**: Ubuntu 22.04 LTS
- **IP**: (ваш IP адрес сервера)

---

## ШАГ 1: Экспорт данных с Railway

### 1.1. Получить DATABASE_URL с Railway

**Вариант A: Через Railway Dashboard**
1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект WhisperFlow
3. Перейдите в **PostgreSQL** сервис
4. Откройте **Connect** → **Postgres Connection URL**
5. Скопируйте `DATABASE_URL` (формат: `postgresql://user:password@host:port/dbname`)

**Вариант B: Через Railway CLI**
```bash
# Установить Railway CLI (если еще не установлено)
npm i -g @railway/cli

# Войти в аккаунт
railway login

# Подключиться к проекту
railway link

# Получить DATABASE_URL
railway variables | grep DATABASE_URL
```

### 1.2. Экспортировать данные

```bash
# Перейти в директорию проекта
cd D:\python-projects\whisperflow

# Установить зависимости (если еще не установлено)
pip install psycopg2-binary

# Установить PostgreSQL клиент (для Windows)
# Скачайте с https://www.postgresql.org/download/windows/
# Или используйте WSL

# Экспортировать данные
python scripts/export_railway_data.py
```

**Или вручную через pg_dump:**
```bash
# Если используете WSL или установили PostgreSQL клиент
pg_dump "postgresql://user:pass@host:port/dbname" -F c -f backup.dump
# Или SQL формат
pg_dump "postgresql://user:pass@host:port/dbname" > backup.sql
```

**После экспорта:**
- ✅ Файл создан в `backups/whisperflow_backup_YYYYMMDD_HHMMSS.sql`
- ✅ Сохраните этот файл, он понадобится для импорта

---

## ШАГ 2: Подключение к VPS и установка Docker

### 2.1. Подключиться к VPS по SSH

```bash
# Подключиться (замените YOUR_IP на IP вашего сервера)
ssh root@YOUR_IP

# Или если используете домен (после настройки DNS)
ssh root@app-toolbox.space
```

**Примечание:** Если это первый раз, система может попросить подтвердить fingerprint - введите `yes`.

### 2.2. Обновить систему

```bash
# Обновить список пакетов
apt update

# Обновить установленные пакеты
apt upgrade -y

# Перезагрузить (если были обновления ядра)
reboot

# После перезагрузки снова подключиться
ssh root@YOUR_IP
```

### 2.3. Установить Docker

```bash
# Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установить Docker Compose Plugin
apt install docker-compose-plugin -y

# Проверить установку
docker --version
docker compose version

# Добавить текущего пользователя в группу docker (если не root)
# usermod -aG docker $USER
# Но так как мы root, это не нужно
```

### 2.4. Настроить firewall

```bash
# Установить UFW (если еще не установлен)
apt install ufw -y

# Разрешить SSH (важно сделать первым!)
ufw allow 22/tcp

# Разрешить HTTP и HTTPS
ufw allow 80/tcp
ufw allow 443/tcp

# Включить firewall
ufw enable

# Проверить статус
ufw status
```

---

## ШАГ 3: Клонирование проекта и настройка

### 3.1. Клонировать проект на VPS

**Вариант A: Если проект на GitHub**
```bash
# Установить Git (если еще не установлен)
apt install git -y

# Клонировать проект
cd /opt
git clone https://github.com/YOUR_USERNAME/whisperflow.git
cd whisperflow
```

**Вариант B: Загрузить проект через SCP (с локального компьютера)**
```bash
# На вашем локальном компьютере (Windows PowerShell или WSL)
# Перейти в директорию проекта
cd D:\python-projects\whisperflow

# Загрузить проект на сервер
scp -r . root@YOUR_IP:/opt/whisperflow

# На сервере перейти в директорию
ssh root@YOUR_IP
cd /opt/whisperflow
```

**Вариант C: Создать проект вручную (скопировать файлы)**
```bash
# На сервере создать директорию
mkdir -p /opt/whisperflow
cd /opt/whisperflow

# Используйте SCP для копирования всех файлов проекта
# Или используйте git clone
```

### 3.2. Создать .env файл

```bash
# Скопировать пример
cp env.example .env

# Отредактировать .env файл
nano .env
```

**Настроить .env файл:**
```env
# Database Configuration для PostgreSQL в Docker
DATABASE_URL=postgresql://whisperflow_user:YOUR_SECURE_PASSWORD@db:5432/whisperflow

# PostgreSQL Password (для docker-compose)
POSTGRES_PASSWORD=YOUR_SECURE_PASSWORD

# Secret Key для JWT токенов (минимум 32 символа)
# Сгенерировать: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=YOUR_GENERATED_SECRET_KEY_HERE_MIN_32_CHARS

# Port
PORT=8000

# Environment
ENVIRONMENT=production
```

**Сгенерировать SECRET_KEY:**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

**Сгенерировать безопасный пароль:**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(24))"
```

**Важно:** 
- ✅ Замените `YOUR_SECURE_PASSWORD` на сгенерированный пароль
- ✅ Замените `YOUR_GENERATED_SECRET_KEY_HERE_MIN_32_CHARS` на сгенерированный ключ
- ✅ Сохраните эти данные в безопасном месте!

### 3.3. Проверить docker-compose.yml

```bash
# Проверить, что docker-compose.yml существует
cat docker-compose.yml

# Должен содержать:
# - сервис app (FastAPI приложение)
# - сервис db (PostgreSQL 15)
```

---

## ШАГ 4: Запуск Docker Compose

### 4.1. Запустить контейнеры

```bash
# Находиться в директории проекта
cd /opt/whisperflow

# Собрать и запустить контейнеры
docker compose up -d --build

# Проверить статус
docker compose ps

# Посмотреть логи
docker compose logs -f
```

**Ожидаемый результат:**
- ✅ Контейнер `db` должен быть `healthy`
- ✅ Контейнер `app` должен быть `running`
- ✅ Оба контейнера должны быть в статусе `Up`

### 4.2. Проверить работу PostgreSQL

```bash
# Подключиться к PostgreSQL
docker compose exec db psql -U whisperflow_user -d whisperflow

# В psql выполнить:
\dt  # Показать таблицы
SELECT COUNT(*) FROM users;  # Проверить пользователей
\q   # Выйти
```

**Если таблиц нет** - это нормально, мы импортируем их в следующем шаге.

### 4.3. Проверить работу приложения

```bash
# Проверить, что приложение отвечает
curl http://localhost:8000/health

# Или
curl http://localhost:8000/
```

---

## ШАГ 5: Импорт данных в PostgreSQL

### 5.1. Загрузить backup файл на сервер

**На вашем локальном компьютере:**
```bash
# Найти файл backup (из ШАГ 1)
# Обычно в backups/whisperflow_backup_YYYYMMDD_HHMMSS.sql

# Загрузить на сервер
scp backups/whisperflow_backup_*.sql root@YOUR_IP:/tmp/backup.sql
```

### 5.2. Импортировать данные

**На сервере:**
```bash
# Перейти в директорию проекта
cd /opt/whisperflow

# Импортировать через Docker Compose
docker compose exec -T db psql -U whisperflow_user -d whisperflow < /tmp/backup.sql

# Или использовать скрипт импорта (если он на сервере)
python3 scripts/import_data.py /tmp/backup.sql --docker
```

### 5.3. Проверить импортированные данные

```bash
# Подключиться к базе
docker compose exec db psql -U whisperflow_user -d whisperflow

# Проверить таблицы
\dt

# Проверить количество записей
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM conversations;
SELECT COUNT(*) FROM transcripts;

# Выйти
\q
```

**Если есть ошибки импорта:**
- Убедитесь, что база данных пустая (или удалите существующие таблицы)
- Проверьте формат файла backup
- Проверьте кодировку файла (должна быть UTF-8)

### 5.4. Перезапустить приложение

```bash
# Перезапустить контейнеры
docker compose restart

# Проверить логи
docker compose logs -f app
```

---

## ШАГ 6: Настройка Nginx и домена

### 6.1. Установить Nginx

```bash
# Установить Nginx
apt install nginx -y

# Проверить статус
systemctl status nginx

# Включить автозапуск
systemctl enable nginx
```

### 6.2. Настроить DNS для домена app-toolbox.space

**В панели управления доменом (где вы покупали домен):**

1. Зайдите в настройки DNS
2. Добавьте A-запись:
   - **Имя**: `@` (или оставьте пустым для корневого домена)
   - **Тип**: `A`
   - **Значение**: `YOUR_IP` (IP вашего VPS)
   - **TTL**: `3600` (или по умолчанию)

3. Добавьте A-запись для www:
   - **Имя**: `www`
   - **Тип**: `A`
   - **Значение**: `YOUR_IP`
   - **TTL**: `3600`

**Проверить DNS:**
```bash
# На вашем локальном компьютере
nslookup app-toolbox.space

# Должен вернуть IP вашего сервера
```

**Примечание:** DNS изменения могут занять от 5 минут до 24 часов.

### 6.3. Создать конфигурацию Nginx

```bash
# Создать конфигурацию для приложения
nano /etc/nginx/sites-available/whisperflow
```

**Вставить следующую конфигурацию:**
```nginx
server {
    listen 80;
    server_name app-toolbox.space www.app-toolbox.space;

    # Максимальный размер загружаемого файла (500MB для аудио)
    client_max_body_size 500M;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для обработки больших файлов
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }

    # Логи
    access_log /var/log/nginx/whisperflow_access.log;
    error_log /var/log/nginx/whisperflow_error.log;
}
```

**Сохранить:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 6.4. Активировать конфигурацию

```bash
# Создать символическую ссылку
ln -s /etc/nginx/sites-available/whisperflow /etc/nginx/sites-enabled/

# Удалить дефолтную конфигурацию (опционально)
rm /etc/nginx/sites-enabled/default

# Проверить конфигурацию
nginx -t

# Если все ОК, перезагрузить Nginx
systemctl reload nginx
```

### 6.5. Проверить работу через домен

```bash
# Проверить, что домен работает
curl http://app-toolbox.space

# Или в браузере открыть:
# http://app-toolbox.space
```

**Если домен не работает:**
- Проверьте DNS записи
- Проверьте, что firewall разрешает порт 80
- Проверьте логи Nginx: `tail -f /var/log/nginx/whisperflow_error.log`

---

## ШАГ 7: Получение SSL сертификата (HTTPS)

### 7.1. Установить Certbot

```bash
# Установить Certbot
apt install certbot python3-certbot-nginx -y
```

### 7.2. Получить SSL сертификат

```bash
# Получить сертификат для домена
certbot --nginx -d app-toolbox.space -d www.app-toolbox.space

# Certbot спросит:
# - Email адрес: введите ваш email
# - Согласиться с условиями: введите Y
# - Подписаться на новости: можете ввести N
# - Редирект HTTP на HTTPS: выберите 2 (Redirect)
```

**Примечание:** Certbot автоматически:
- Получит SSL сертификат от Let's Encrypt
- Настроит HTTPS в Nginx
- Настроит автоматическое обновление сертификата

### 7.3. Проверить HTTPS

```bash
# Проверить, что HTTPS работает
curl https://app-toolbox.space

# Или в браузере:
# https://app-toolbox.space
```

### 7.4. Проверить автообновление сертификата

```bash
# Проверить статус автообновления
systemctl status certbot.timer

# Протестировать обновление (без реального обновления)
certbot renew --dry-run
```

---

## ШАГ 8: Финальная проверка и тестирование

### 8.1. Проверить работу всех компонентов

```bash
# Проверить контейнеры
docker compose ps

# Проверить использование ресурсов
docker stats

# Проверить логи приложения
docker compose logs app --tail=50

# Проверить логи PostgreSQL
docker compose logs db --tail=50
```

### 8.2. Проверить веб-интерфейс

**В браузере открыть:**
- ✅ https://app-toolbox.space - главная страница
- ✅ https://app-toolbox.space/login - страница входа
- ✅ Проверить, что HTTPS работает (замочек в адресной строке)

### 8.3. Протестировать функциональность

- [ ] Вход в систему (существующий пользователь из Railway)
- [ ] Загрузка аудио файла
- [ ] Транскрипция работает
- [ ] AI анализ работает (sentiment, summarization)
- [ ] Экспорт результатов работает
- [ ] Все данные из Railway присутствуют

### 8.4. Настроить автоматические бэкапы (рекомендуется)

```bash
# Создать скрипт бэкапа
nano /opt/backup-whisperflow.sh
```

**Содержимое скрипта:**
```bash
#!/bin/bash
BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

# Бэкап базы данных
docker compose -f /opt/whisperflow/docker-compose.yml exec -T db pg_dump -U whisperflow_user whisperflow > $BACKUP_DIR/db_$DATE.sql

# Бэкап файлов (если нужно)
# tar -czf $BACKUP_DIR/storage_$DATE.tar.gz -C /opt/whisperflow/backend storage/

# Удалить старые бэкапы (старше 7 дней)
find $BACKUP_DIR -type f -mtime +7 -delete

echo "Backup completed: $BACKUP_DIR/db_$DATE.sql"
```

```bash
# Сделать скрипт исполняемым
chmod +x /opt/backup-whisperflow.sh

# Добавить в cron (ежедневно в 3:00)
crontab -e

# Добавить строку:
0 3 * * * /opt/backup-whisperflow.sh >> /var/log/whisperflow-backup.log 2>&1
```

---

## ✅ Чеклист миграции

- [ ] Экспортированы данные с Railway
- [ ] Подключен VPS по SSH
- [ ] Установлен Docker и Docker Compose
- [ ] Настроен firewall
- [ ] Проект склонирован/загружен на VPS
- [ ] Создан и настроен .env файл
- [ ] Запущены контейнеры Docker Compose
- [ ] PostgreSQL работает и доступна
- [ ] Данные импортированы в PostgreSQL
- [ ] Установлен и настроен Nginx
- [ ] Настроены DNS записи для домена
- [ ] Домен app-toolbox.space работает через HTTP
- [ ] Получен SSL сертификат
- [ ] HTTPS работает
- [ ] Протестирована вся функциональность
- [ ] Настроены автоматические бэкапы
- [ ] Railway можно отключить (после тестирования)

---

## 🔧 Решение проблем

### Проблема: Контейнер не запускается

```bash
# Проверить логи
docker compose logs app
docker compose logs db

# Проверить конфигурацию
docker compose config

# Перезапустить
docker compose restart
```

### Проблема: База данных не подключается

```bash
# Проверить статус PostgreSQL
docker compose ps db
docker compose logs db

# Проверить .env файл
cat .env | grep DATABASE_URL

# Проверить подключение
docker compose exec db psql -U whisperflow_user -d whisperflow
```

### Проблема: Nginx не работает

```bash
# Проверить конфигурацию
nginx -t

# Проверить статус
systemctl status nginx

# Проверить логи
tail -f /var/log/nginx/whisperflow_error.log
```

### Проблема: SSL не работает

```bash
# Проверить сертификаты
certbot certificates

# Обновить сертификат вручную
certbot renew

# Проверить конфигурацию Nginx
nginx -t
```

### Проблема: Недостаточно памяти

```bash
# Проверить использование памяти
free -h
docker stats

# Если не хватает, можно:
# 1. Увеличить swap
# 2. Использовать более легкие модели Whisper
# 3. Обновить VPS до 8GB
```

---

## 📝 Полезные команды

```bash
# Перезапустить все контейнеры
docker compose restart

# Остановить все контейнеры
docker compose down

# Запустить все контейнеры
docker compose up -d

# Посмотреть логи
docker compose logs -f

# Обновить проект (если используете git)
cd /opt/whisperflow
git pull
docker compose up -d --build

# Подключиться к контейнеру приложения
docker compose exec app bash

# Подключиться к PostgreSQL
docker compose exec db psql -U whisperflow_user -d whisperflow
```

---

## 🎉 Готово!

После выполнения всех шагов ваш WhisperFlow будет работать на:
- **Домен**: https://app-toolbox.space
- **VPS**: Ukraine.com.ua (Киев, Украина)
- **PostgreSQL**: В Docker контейнере
- **HTTPS**: Настроен через Let's Encrypt

**Следующие шаги:**
- Регулярно проверяйте логи: `docker compose logs`
- Мониторьте использование ресурсов: `docker stats`
- Регулярно делайте бэкапы
- Обновляйте систему: `apt update && apt upgrade`

**Удачной миграции!** 🚀
