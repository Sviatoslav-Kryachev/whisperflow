# 🇺🇦 Українські провайдери хостингу для WhisperFlow

## Рекомендовані провайдери

### 1. **HostPro** (hostpro.ua) ⭐ РЕКОМЕНДОВАНО
**Переваги:**
- ✅ Підтримка Docker контейнерів
- ✅ PostgreSQL база даних
- ✅ Безкоштовний SSL сертифікат (Let's Encrypt)
- ✅ VPS/VDS з повним контролем
- ✅ Українська підтримка 24/7
- ✅ Добрі ціни для українських клієнтів
- ✅ Можливість встановлення власного сервера

**Підхідні тарифи:**
- **⚠️ VPS 2GB** (~264 грн/міс) - **НЕ рекомендовано** (буде недостатньо для Whisper)
- **✅ VPS 4GB** (12.02 €/міс ≈ 480-500 грн/міс) - **мінімум** (рекомендовано)
- **⭐ VPS 8GB** (23.84 €/міс ≈ 950-1000 грн/міс) - **оптимально** для продакшену
- **Dedicated Server** - для високого навантаження

**⚠️ ВАЖЛИВО:** Див. `VPS_MEMORY_REQUIREMENTS.md` для детального аналізу вимог до пам'яті.

**Як розгорнути:**
1. Замовити VPS з Ubuntu/Debian
2. Встановити Docker та Docker Compose
3. Розгорнути через Dockerfile (як на Railway)

---

### 2. **Ukraine.com.ua** (ukraine.com.ua)
**Переваги:**
- ✅ VPS/VDS сервери
- ✅ PostgreSQL підтримка
- ✅ SSL сертифікати
- ✅ Українська техпідтримка
- ✅ Добрі локації дата-центрів в Україні

**Тарифи:** від 400 грн/міс

---

### 3. **Mirohost** (mirohost.net)
**Переваги:**
- ✅ VPS сервери з повним root доступом
- ✅ Docker підтримка
- ✅ PostgreSQL можна встановити самостійно
- ✅ SSL сертифікати
- ✅ Українська підтримка

**Тарифи:** від 300 грн/міс

---

### 4. **Hostland** (hostland.ua)
**Переваги:**
- ✅ VPS/VDS сервери
- ✅ Docker підтримка
- ✅ PostgreSQL
- ✅ SSL сертифікати
- ✅ Українська техпідтримка

**Тарифи:** від 450 грн/міс

---

### 5. **CloudSigma** (cloudsigma.com) - має представництво в Україні
**Переваги:**
- ✅ Cloud VPS з повним контролем
- ✅ Docker підтримка
- ✅ PostgreSQL
- ✅ Гнучкі тарифи (pay-as-you-go)
- ✅ Добрі технічні характеристики

---

## 🚀 Інструкція по міграції з Railway

### Варіант 1: VPS з Docker (найпростіший)

#### Крок 1: Підготовка VPS
```bash
# Підключитися до VPS через SSH
ssh root@your-server-ip

# Оновити систему
apt update && apt upgrade -y

# Встановити Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Встановити Docker Compose
apt install docker-compose -y
```

#### Крок 2: Встановити PostgreSQL
```bash
# Встановити PostgreSQL
apt install postgresql postgresql-contrib -y

# Створити базу даних
sudo -u postgres psql
CREATE DATABASE whisperflow;
CREATE USER whisperflow_user WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE whisperflow TO whisperflow_user;
\q
```

#### Крок 3: Налаштувати SSL (Let's Encrypt)
```bash
# Встановити Certbot
apt install certbot python3-certbot-nginx -y

# Або якщо використовуєте Apache
apt install certbot python3-certbot-apache -y
```

#### Крок 4: Створити docker-compose.yml
Створіть файл `docker-compose.yml` в корені проекту:

```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://whisperflow_user:your_secure_password@db:5432/whisperflow
      - SECRET_KEY=your-secret-key-here
    volumes:
      - ./backend/storage:/app/backend/storage
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=whisperflow
      - POSTGRES_USER=whisperflow_user
      - POSTGRES_PASSWORD=your_secure_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
```

#### Крок 5: Налаштувати Nginx як reverse proxy
```bash
# Встановити Nginx
apt install nginx -y

# Створити конфігурацію
nano /etc/nginx/sites-available/whisperflow
```

Вміст конфігурації Nginx:
```nginx
server {
    listen 80;
    server_name your-domain.com.ua;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
# Активувати конфігурацію
ln -s /etc/nginx/sites-available/whisperflow /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx

# Отримати SSL сертифікат
certbot --nginx -d your-domain.com.ua
```

#### Крок 6: Розгорнути проект
```bash
# Клонувати репозиторій
git clone https://github.com/your-username/whisperflow.git
cd whisperflow

# Запустити через Docker Compose
docker-compose up -d --build

# Перевірити логи
docker-compose logs -f
```

---

### Варіант 2: Використання готових Docker-хостингів

Деякі провайдери пропонують готові рішення для Docker:

1. **HostPro Docker Hosting** - спеціальний тариф для Docker
2. **DigitalOcean** (має українську підтримку) - App Platform підтримує Docker
3. **Render.com** (альтернатива Railway) - безкоштовний тариф доступний

---

## 📋 Чеклист міграції

- [ ] Вибрати провайдера та тариф
- [ ] Замовити VPS/хостинг
- [ ] Налаштувати домен (якщо потрібно)
- [ ] Встановити Docker та Docker Compose
- [ ] Встановити PostgreSQL
- [ ] Налаштувати Nginx/Apache
- [ ] Отримати SSL сертифікат
- [ ] Експортувати дані з Railway PostgreSQL
- [ ] Імпортувати дані в нову базу
- [ ] Розгорнути проект через Docker
- [ ] Налаштувати автоматичні бекапи
- [ ] Налаштувати моніторинг (опціонально)

---

## 🔐 Безпека

1. **Firewall:**
```bash
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

2. **Оновлення системи:**
```bash
apt update && apt upgrade -y
```

3. **Сильні паролі** для всіх сервісів

4. **Регулярні бекапи** бази даних

---

## 💰 Порівняння вартості

| Провайдер | Мінімальний тариф | Підтримка Docker | PostgreSQL | SSL |
|-----------|-------------------|------------------|------------|-----|
| HostPro | ~500 грн/міс | ✅ | ✅ | ✅ |
| Ukraine.com.ua | ~400 грн/міс | ✅ | ✅ | ✅ |
| Mirohost | ~300 грн/міс | ✅ | ⚠️ (самостійно) | ✅ |
| Hostland | ~450 грн/міс | ✅ | ✅ | ✅ |
| Railway | $5-20/міс | ✅ | ✅ | ✅ |

---

## 📞 Підтримка

Більшість українських провайдерів мають:
- Українську техпідтримку
- Телефонну підтримку
- Онлайн-чат
- Email підтримку

---

## 🎯 Рекомендація

**Для WhisperFlow рекомендую HostPro або Ukraine.com.ua** - вони мають найкращу підтримку Docker та PostgreSQL, а також добру техпідтримку українською мовою.

---

## 📝 Додаткові ресурси

- [Docker документація](https://docs.docker.com/)
- [Nginx документація](https://nginx.org/ru/docs/)
- [Let's Encrypt документація](https://letsencrypt.org/docs/)
- [PostgreSQL документація](https://www.postgresql.org/docs/)

