# 🤖 Автоматическая настройка VPS для WhisperFlow

## ✅ Что я сделал для вас:

Я создал **автоматические скрипты**, которые делают большую часть работы за вас!

---

## 🚀 Быстрый старт (3 команды!)

### ШАГ 1: Загрузить скрипты на VPS

**На вашем локальном компьютере:**
```bash
cd D:\python-projects\whisperflow

# Закоммитить и запушить скрипты в GitHub (если еще не сделали)
git add scripts/
git commit -m "Добавлены автоматические скрипты настройки"
git push origin main
```

**На VPS (в терминале VPS):**
```bash
cd /opt/whisperflow

# Подтянуть последние изменения с GitHub (включая скрипты)
git pull origin main

# Сделать скрипты исполняемыми
chmod +x scripts/*.sh
```

---

### ШАГ 2: Запустить автоматическую настройку PostgreSQL

**На VPS (в терминале VPS):**
```bash
cd /opt/whisperflow

# Запустить скрипт настройки PostgreSQL
bash scripts/setup_postgres.sh
```

**Что делает скрипт:**
- ✅ Генерирует безопасный пароль автоматически
- ✅ Создает базу данных `whisperflow_db`
- ✅ Создает пользователя `whisperflow_user`
- ✅ Выдает все необходимые права
- ✅ Проверяет подключение
- ✅ Создает/обновляет `.env` файл с правильными настройками
- ✅ Генерирует SECRET_KEY автоматически

**Результат:** Скрипт выведет пароль базы данных - **СОХРАНИТЕ ЕГО!**

---

### ШАГ 3: Запустить полную автоматическую настройку (опционально)

**Если хотите все сделать одной командой (Docker + PostgreSQL + запуск):**

**На VPS (в терминале VPS):**
```bash
cd /opt/whisperflow

# Запустить полную автоматическую настройку
bash scripts/complete_setup.sh
```

**Что делает скрипт:**
- ✅ Обновляет систему
- ✅ Устанавливает необходимые пакеты
- ✅ Устанавливает Docker и Docker Compose
- ✅ Настраивает firewall
- ✅ Устанавливает PostgreSQL (если еще не установлен)
- ✅ Настраивает PostgreSQL (вызывает setup_postgres.sh)
- ✅ Проверяет проект и подтягивает изменения с GitHub
- ✅ Создает/проверяет .env файл
- ✅ Собирает Docker образы
- ✅ Запускает контейнеры
- ✅ Проверяет работу приложения

**Время выполнения:** ~5-10 минут (в зависимости от скорости интернета)

---

## 📋 Или пошагово (если хотите контролировать каждый шаг):

### Вариант A: Только PostgreSQL (если Docker уже установлен)

```bash
cd /opt/whisperflow
bash scripts/setup_postgres.sh
```

### Вариант B: Только Docker и запуск (если PostgreSQL уже настроен)

```bash
cd /opt/whisperflow

# Установить Docker (если еще не установлен)
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
rm get-docker.sh
apt install docker-compose-plugin -y

# Запустить приложение
docker compose up -d --build

# Проверить логи
docker compose logs -f app
```

---

## 🔍 Что делать, если скрипт выдает ошибки:

### 1. Скопировать полный вывод ошибки

**Пришлите мне:**
- Полный вывод команды (все что было в терминале)
- Или скриншот ошибки

**Я помогу:**
- Проанализировать ошибку
- Предложить исправление
- Создать патч для скрипта

---

### 2. Проверить вручную

**Если скрипт не работает, выполните команды вручную:**

```bash
# Проверить, что PostgreSQL работает
sudo systemctl status postgresql

# Проверить, что Docker установлен
docker --version
docker compose version

# Проверить, что проект на месте
cd /opt/whisperflow
ls -la

# Проверить .env файл
cat .env | grep DATABASE_URL
```

**Пришлите мне вывод этих команд, и я помогу исправить!**

---

## 📝 Что вам нужно будет сделать вручную:

### 1. Подключиться к VPS по SSH
```bash
ssh root@YOUR_VPS_IP
```

### 2. Выполнить команды (скопировать-вставить)

Просто копируйте команды, которые я даю, и вставляйте в терминал VPS.

### 3. Прислать логи, если что-то не работает

**Если команда выдает ошибку:**
```bash
# Скопируйте весь вывод терминала и пришлите мне

# Или сохраните в файл:
команда 2>&1 | tee error.log

# И пришлите содержимое error.log
```

---

## ✅ После успешного запуска:

**Проверить работу:**
```bash
# На VPS
curl http://localhost:8000/health

# Или в браузере (если настроен Nginx)
http://YOUR_VPS_IP:8000
```

**Посмотреть логи:**
```bash
cd /opt/whisperflow
docker compose logs -f app
```

**Проверить статус:**
```bash
docker compose ps
```

---

## 🆘 Если что-то не работает:

### Пришлите мне:

1. **Вывод команды с ошибкой:**
   ```bash
   # Скопируйте весь вывод терминала
   ```

2. **Логи контейнеров:**
   ```bash
   docker compose logs app > app_logs.txt
   docker compose logs db > db_logs.txt 2>&1 || echo "DB not in docker" > db_logs.txt
   
   # Пришлите содержимое этих файлов
   ```

3. **Статус контейнеров:**
   ```bash
   docker compose ps
   docker ps -a
   ```

4. **Проверка .env:**
   ```bash
   cat /opt/whisperflow/.env | grep -v "PASSWORD\|SECRET"  # Без паролей!
   ```

5. **Проверка PostgreSQL:**
   ```bash
   sudo systemctl status postgresql
   psql -U whisperflow_user -d whisperflow_db -c "\dt"
   ```

---

## 🎯 План действий прямо сейчас:

1. **Подключитесь к VPS по SSH**
2. **Выполните команды из ШАГ 1-2 выше**
3. **Пришлите мне вывод терминала** (особенно если есть ошибки)
4. **Я проанализирую и помогу исправить**

---

**Готовы начать?** Выполните ШАГ 1 (загрузить скрипты) и ШАГ 2 (запустить setup_postgres.sh), и пришлите мне результат! 🚀
