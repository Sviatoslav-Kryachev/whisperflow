# WhisperFlow

Приложение для транскрипции аудио с использованием OpenAI Whisper, с поддержкой AI-анализа и синхронизированного аудиоплеера.

## Возможности

- 🎙️ **Транскрипция аудио** - преобразование речи в текст с таймкодами
- 🤖 **AI-анализ** - резюме, ключевые слова, анализ тональности, классификация, перевод
- 🎧 **Аудиоплеер с синхронизацией** - прослушивание с подсветкой текущего сегмента
- 📁 **Организация** - папки для группировки транскрипций
- 📤 **Экспорт** - DOCX, XLSX, SRT, TXT форматы
- ⌨️ **Горячие клавиши** - быстрая навигация и управление

## Установка

1. Установите зависимости:
```bash
cd backend
pip install -r requirements.txt
```

**Опционально** (для полной функциональности AI):
```bash
pip install transformers torch sentencepiece protobuf
pip install googletrans==4.0.0rc1
```

2. Выполните миграцию базы данных:
```bash
cd backend
python migrate_db.py
```

## Запуск

### Вариант 1: Использование скрипта (Windows)
```bash
start.bat
```

### Вариант 2: Ручной запуск
```bash
cd backend
python -m uvicorn app.main:app --reload
```

### Вариант 3: С указанием хоста и порта
```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

После запуска сервер будет доступен по адресу: **http://127.0.0.1:8000**

## Структура проекта

- `backend/` - FastAPI бэкенд
  - `app/routes/` - API endpoints
  - `app/models.py` - модели базы данных
  - `app/ai_service.py` - AI-сервисы
  - `storage/audio/` - загруженные аудиофайлы
  - `storage/transcripts/` - готовые транскрипции
- `frontend/` - HTML/CSS/JS фронтенд
  - `dashboard.html` - главная страница
  - `js/` - JavaScript модули
  - `css/` - стили

## Основные функции

### Транскрипция аудио
- Поддержка форматов: MP3, WAV, OGG, M4A, FLAC, WEBM
- Выбор модели Whisper (tiny, base, small, medium, large)
- Автоопределение языка или ручной выбор
- Отслеживание прогресса обработки

### AI-анализ транскрипций
- **Резюме** - автоматическое создание краткого резюме
- **Ключевые слова** - извлечение важных слов
- **Анализ тональности** - определение позитивности/негативности
- **Классификация** - автоматическая категоризация (встреча, интервью, лекция и т.д.)
- **Перевод** - перевод на 20+ языков

### Аудиоплеер с синхронизацией
- Просмотр транскрипции с синхронизированным аудио
- Подсветка текущего сегмента во время воспроизведения
- Клик по тексту для перехода к нужному моменту
- Контроль скорости воспроизведения (0.5x - 2x)
- Горячие клавиши для управления

### Организация
- Создание папок для группировки транскрипций
- Фильтрация по папкам
- Перемещение файлов между папками

### Экспорт
- DOCX - Microsoft Word
- XLSX - Microsoft Excel
- SRT - субтитры для YouTube
- TXT - простой текст

## API Endpoints

### Транскрипция
- `POST /upload` - загрузка аудиофайла
- `GET /status/{file_id}` - статус обработки
- `GET /transcript/{file_id}` - получить транскрипцию
- `GET /audio/{file_id}` - получить аудиофайл
- `POST /retry/{file_id}` - повторить обработку

### Транскрипции
- `GET /transcripts/list` - список транскрипций
- `POST /transcripts/rename/{file_id}` - переименовать
- `DELETE /transcripts/delete/{file_id}` - удалить
- `POST /transcripts/move/{file_id}` - переместить в папку

### Папки
- `GET /folders/list` - список папок
- `POST /folders/create` - создать папку
- `POST /folders/rename/{folder_id}` - переименовать
- `DELETE /folders/delete/{folder_id}` - удалить

### Экспорт
- `GET /export/txt/{file_id}` - экспорт в TXT
- `GET /export/docx/{file_id}` - экспорт в DOCX
- `GET /export/xlsx/{file_id}` - экспорт в XLSX
- `GET /export/srt/{file_id}` - экспорт в SRT

### AI-анализ
- `POST /ai/summary/{file_id}` - создать резюме
- `POST /ai/keywords/{file_id}` - извлечь ключевые слова
- `POST /ai/sentiment/{file_id}` - анализ тональности
- `POST /ai/classify/{file_id}` - классификация
- `POST /ai/translate/{file_id}` - перевод
- `POST /ai/analyze-all/{file_id}` - выполнить все анализы
- `GET /ai/data/{file_id}` - получить все AI-данные
- `GET /ai/languages` - список поддерживаемых языков

## Горячие клавиши (в аудиоплеере)

- `Space` - пауза/воспроизведение
- `←` - назад на 5 секунд
- `→` - вперёд на 5 секунд
- `↑` - увеличить скорость
- `↓` - уменьшить скорость
- `Escape` - закрыть модальное окно

## Документация

- [AI Features API](backend/AI_FEATURES.md) - полная документация AI-фич
- [AI Quick Start](backend/AI_QUICKSTART.md) - быстрый старт для AI
- [AI Implementation](backend/AI_IMPLEMENTATION.md) - технические детали

## Требования

- Python 3.8+
- FastAPI
- OpenAI Whisper
- SQLite (встроенная БД)
- Опционально: transformers, googletrans для AI-фич

## Примечания

- Все файлы обрабатываются локально на вашем компьютере
- Данные хранятся в `backend/storage/`
- База данных: `backend/db.sqlite3`
- Первая загрузка AI-моделей может занять время (загружаются из интернета)

## Лицензия

MIT

Приложение для транскрипции аудио с использованием OpenAI Whisper, с поддержкой AI-анализа и синхронизированного аудиоплеера.

## Возможности

- 🎙️ **Транскрипция аудио** - преобразование речи в текст с таймкодами
- 🤖 **AI-анализ** - резюме, ключевые слова, анализ тональности, классификация, перевод
- 🎧 **Аудиоплеер с синхронизацией** - прослушивание с подсветкой текущего сегмента
- 📁 **Организация** - папки для группировки транскрипций
- 📤 **Экспорт** - DOCX, XLSX, SRT, TXT форматы
- ⌨️ **Горячие клавиши** - быстрая навигация и управление

## Установка

1. Установите зависимости:
```bash
cd backend
pip install -r requirements.txt
```

**Опционально** (для полной функциональности AI):
```bash
pip install transformers torch sentencepiece protobuf
pip install googletrans==4.0.0rc1
```

2. Выполните миграцию базы данных:
```bash
cd backend
python migrate_db.py
```

## Запуск

### Вариант 1: Использование скрипта (Windows)
```bash
start.bat
```

### Вариант 2: Ручной запуск
```bash
cd backend
python -m uvicorn app.main:app --reload
```

### Вариант 3: С указанием хоста и порта
```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

После запуска сервер будет доступен по адресу: **http://127.0.0.1:8000**

## Структура проекта

- `backend/` - FastAPI бэкенд
  - `app/routes/` - API endpoints
  - `app/models.py` - модели базы данных
  - `app/ai_service.py` - AI-сервисы
  - `storage/audio/` - загруженные аудиофайлы
  - `storage/transcripts/` - готовые транскрипции
- `frontend/` - HTML/CSS/JS фронтенд
  - `dashboard.html` - главная страница
  - `js/` - JavaScript модули
  - `css/` - стили

## Основные функции

### Транскрипция аудио
- Поддержка форматов: MP3, WAV, OGG, M4A, FLAC, WEBM
- Выбор модели Whisper (tiny, base, small, medium, large)
- Автоопределение языка или ручной выбор
- Отслеживание прогресса обработки

### AI-анализ транскрипций
- **Резюме** - автоматическое создание краткого резюме
- **Ключевые слова** - извлечение важных слов
- **Анализ тональности** - определение позитивности/негативности
- **Классификация** - автоматическая категоризация (встреча, интервью, лекция и т.д.)
- **Перевод** - перевод на 20+ языков

### Аудиоплеер с синхронизацией
- Просмотр транскрипции с синхронизированным аудио
- Подсветка текущего сегмента во время воспроизведения
- Клик по тексту для перехода к нужному моменту
- Контроль скорости воспроизведения (0.5x - 2x)
- Горячие клавиши для управления

### Организация
- Создание папок для группировки транскрипций
- Фильтрация по папкам
- Перемещение файлов между папками

### Экспорт
- DOCX - Microsoft Word
- XLSX - Microsoft Excel
- SRT - субтитры для YouTube
- TXT - простой текст

## API Endpoints

### Транскрипция
- `POST /upload` - загрузка аудиофайла
- `GET /status/{file_id}` - статус обработки
- `GET /transcript/{file_id}` - получить транскрипцию
- `GET /audio/{file_id}` - получить аудиофайл
- `POST /retry/{file_id}` - повторить обработку

### Транскрипции
- `GET /transcripts/list` - список транскрипций
- `POST /transcripts/rename/{file_id}` - переименовать
- `DELETE /transcripts/delete/{file_id}` - удалить
- `POST /transcripts/move/{file_id}` - переместить в папку

### Папки
- `GET /folders/list` - список папок
- `POST /folders/create` - создать папку
- `POST /folders/rename/{folder_id}` - переименовать
- `DELETE /folders/delete/{folder_id}` - удалить

### Экспорт
- `GET /export/txt/{file_id}` - экспорт в TXT
- `GET /export/docx/{file_id}` - экспорт в DOCX
- `GET /export/xlsx/{file_id}` - экспорт в XLSX
- `GET /export/srt/{file_id}` - экспорт в SRT

### AI-анализ
- `POST /ai/summary/{file_id}` - создать резюме
- `POST /ai/keywords/{file_id}` - извлечь ключевые слова
- `POST /ai/sentiment/{file_id}` - анализ тональности
- `POST /ai/classify/{file_id}` - классификация
- `POST /ai/translate/{file_id}` - перевод
- `POST /ai/analyze-all/{file_id}` - выполнить все анализы
- `GET /ai/data/{file_id}` - получить все AI-данные
- `GET /ai/languages` - список поддерживаемых языков

## Горячие клавиши (в аудиоплеере)

- `Space` - пауза/воспроизведение
- `←` - назад на 5 секунд
- `→` - вперёд на 5 секунд
- `↑` - увеличить скорость
- `↓` - уменьшить скорость
- `Escape` - закрыть модальное окно

## Документация

- [AI Features API](backend/AI_FEATURES.md) - полная документация AI-фич
- [AI Quick Start](backend/AI_QUICKSTART.md) - быстрый старт для AI
- [AI Implementation](backend/AI_IMPLEMENTATION.md) - технические детали

## Требования

- Python 3.8+
- FastAPI
- OpenAI Whisper
- SQLite (встроенная БД)
- Опционально: transformers, googletrans для AI-фич

## Примечания

- Все файлы обрабатываются локально на вашем компьютере
- Данные хранятся в `backend/storage/`
- База данных: `backend/db.sqlite3`
- Первая загрузка AI-моделей может занять время (загружаются из интернета)

## Лицензия

MIT
