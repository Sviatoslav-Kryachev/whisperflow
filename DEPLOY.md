# 🚀 Инструкция по деплою на Railway

## Шаг 1: Подготовка к коммиту

Убедитесь, что все изменения сохранены:

```bash
# Проверьте статус
git status
```

## Шаг 2: Коммит и пуш на GitHub

```bash
# Добавить все файлы
git add .

# Закоммитить
git commit -m "Optimize Whisper with faster-whisper and GPU support, add Railway deploy files"

# Запушить на GitHub
git push
```

## Шаг 3: Деплой на Railway

1. **Откройте Railway**: https://railway.app
2. **Войдите через GitHub** (Login with GitHub)
3. **Создайте новый проект**:
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `whisperflow`
4. **Railway автоматически**:
   - Определит Python проект (благодаря `requirements.txt` в корне)
   - Найдёт `Procfile` в корне
   - Установит зависимости из `backend/requirements.txt`
   - Определит GPU (если доступен)
   - Развернёт проект

**Примечание**: Если Railway всё ещё не определяет проект:
   - В настройках проекта найдите "Root Directory"
   - Установите: `.` (корень проекта)

## Шаг 4: Проверка деплоя

После деплоя Railway предоставит URL типа:
```
https://whisperflow-production.up.railway.app
```

Откройте этот URL в браузере - должно работать! 🎉

## Важные замечания

- **GPU**: Railway автоматически определит и использует GPU, если доступен
- **База данных**: SQLite будет создана автоматически при первом запуске
- **Storage**: Папка `storage/` будет создана автоматически
- **Переменные окружения**: Если нужны секретные ключи, добавьте их в настройках Railway (Settings → Variables)

## Структура файлов для Railway

- `Procfile` в корне - указывает Railway как запускать приложение
- `requirements.txt` в корне - указывает на `backend/requirements.txt` для определения Python проекта
- `backend/Procfile` - альтернативный вариант (если указать Root Directory = `backend`)

## Проверка работы

После деплоя проверьте:
- ✅ Главная страница открывается
- ✅ Загрузка файлов работает
- ✅ Транскрипция запускается
- ✅ В логах Railway видно "GPU" или "CPU" (проверьте логи)

## Логи Railway

Для просмотра логов:
- Railway Dashboard → Ваш проект → Deployments → View Logs
- Ищите строки типа: "Loading model ... on cuda" или "on cpu"

---

**Готово!** Ваш проект должен работать в 3-5 раз быстрее на Railway с GPU! ⚡

