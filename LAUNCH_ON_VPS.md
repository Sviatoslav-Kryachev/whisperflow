# 🚀 Запуск проекта на VPS - Пошаговая инструкция

## 📋 Что уже готово:
- ✅ VPS подключен по SSH
- ✅ PostgreSQL установлен на VPS
- ✅ Проект в `/opt/whisperflow`

---

## 🎯 ШАГИ для запуска проекта (выполняйте по порядку)

### ШАГ 1: Подтянуть последние изменения с GitHub

**На VPS (в терминале):**
```bash
cd /opt/whisperflow

# Подтянуть последние изменения
git pull origin main

# Проверить, что изменения получены
git log --oneline -3
```

---

### ШАГ 2: Настроить PostgreSQL (автоматически)

**На VPS:**
```bash
cd /opt/whisperflow

# Сделать скрипты исполняемыми
chmod +x scripts/*.sh

# Запустить автоматическую настройку PostgreSQL
bash scripts/setup_postgres.sh
```

**Что делает скрипт:**
- ✅ Генерирует безопасный пароль автоматически
- ✅ Создает базу данных `whisperflow_db`
- ✅ Создает пользователя `whisperflow_user`
- ✅ Выдает все необходимые права
- ✅ Создает `.env` файл с правильными настройками
- ✅ Генерирует SECRET_KEY

**⚠️ ВАЖНО:** Скрипт выведет пароль базы данных - **СОХРАНИТЕ ЕГО!**

**После выполнения скрипта проверить:**
```bash
# Проверить, что .env файл создан
cat .env | grep DATABASE_URL

# Проверить подключение к базе данных
psql -U whisperflow_user -d whisperflow_db -c "SELECT 1;"
```

---

### ШАГ 3: Установить Docker и Docker Compose (если еще не установлены)

**На VPS:**
```bash
# Проверить, установлен ли Docker
docker --version

# Если Docker не установлен, установить:
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
rm get-docker.sh

# Установить Docker Compose Plugin
apt update
apt install docker-compose-plugin -y

# Проверить версии
docker --version
docker compose version
```

---

### ШАГ 4: Проверить docker-compose.yml

**На VPS:**
```bash
cd /opt/whisperflow

# Проверить, что docker-compose.yml настроен правильно
cat docker-compose.yml | head -25

# Должно быть:
# - network_mode: host
# - DATABASE_URL с localhost:5432/whisperflow_db
```

**Если docker-compose.yml не обновлен:**
```bash
# Подтянуть последние изменения еще раз
git pull origin main
```

---

### ШАГ 5: Запустить приложение

**На VPS:**
```bash
cd /opt/whisperflow

# Собрать Docker образы
docker compose build

# Запустить контейнеры
docker compose up -d

# Проверить статус контейнеров
docker compose ps

# Посмотреть логи (первые 50 строк)
docker compose logs app --tail=50
```

**Ожидаемый результат:**
- ✅ Контейнер `app` должен быть в статусе `Up`
- ✅ В логах должно быть: "Using PostgreSQL database from DATABASE_URL"
- ✅ Нет критических ошибок

**Проверить работу приложения:**
```bash
# Проверить health endpoint
curl http://localhost:8000/health

# Или проверить главную страницу
curl http://localhost:8000/
```

**Если приложение не отвечает, посмотреть логи:**
```bash
docker compose logs app -f
```

---

### ШАГ 6: Настроить Nginx (для домена app-toolbox.space)

**На VPS:**
```bash
# Установить Nginx (если еще не установлен)
apt install nginx -y

# Создать конфигурацию для WhisperFlow
nano /etc/nginx/sites-available/whisperflow
```

**Вставить следующую конфигурацию:**
```nginx
server {
    listen 80;
    server_name app-toolbox.space www.app-toolbox.space;
    
    # Максимальный размер загружаемого файла (500MB для аудио)
    client_max_body_size 500M;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для обработки больших файлов
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }

    # Логи
    access_log /var/log/nginx/whisperflow_access.log;
    error_log /var/log/nginx/whisperflow_error.log;
}
```

**Сохранить:** `Ctrl+O`, `Enter`, `Ctrl+X`

**Активировать конфигурацию:**
```bash
# Создать символическую ссылку
ln -s /etc/nginx/sites-available/whisperflow /etc/nginx/sites-enabled/

# Удалить дефолтную конфигурацию (опционально)
rm /etc/nginx/sites-enabled/default

# Проверить конфигурацию
nginx -t

# Перезагрузить Nginx
systemctl reload nginx

# Проверить статус
systemctl status nginx
```

---

### ШАГ 7: Настроить DNS для домена app-toolbox.space

**В панели управления доменом (где вы покупали app-toolbox.space):**

1. Зайти в настройки DNS
2. Добавить A-запись для корневого домена:
   - **Имя**: `@` (или оставить пустым)
   - **Тип**: `A`
   - **Значение**: `YOUR_VPS_IP` (IP вашего VPS)
   - **TTL**: `3600`

3. Добавить A-запись для www:
   - **Имя**: `www`
   - **Тип**: `A`
   - **Значение**: `YOUR_VPS_IP`
   - **TTL**: `3600`

**Проверить DNS (может занять 5-60 минут):**
```bash
# На VPS или локальном компьютере
nslookup app-toolbox.space

# Должен вернуть IP вашего VPS
```

**Проверить работу домена:**
```bash
# После распространения DNS
curl http://app-toolbox.space

# Или в браузере:
# http://app-toolbox.space
```

---

### ШАГ 8: Получить SSL сертификат (HTTPS)

**На VPS:**
```bash
# Установить Certbot
apt install certbot python3-certbot-nginx -y

# Получить SSL сертификат для домена
certbot --nginx -d app-toolbox.space -d www.app-toolbox.space

# Certbot спросит:
# - Email адрес: введите ваш email
# - Согласие с условиями: введите Y
# - Подписка на новости: можете ввести N
# - Редирект HTTP на HTTPS: выберите 2 (Redirect)
```

**Проверить HTTPS:**
```bash
# Проверить, что HTTPS работает
curl https://app-toolbox.space

# Или в браузере:
# https://app-toolbox.space
```

**Проверить автообновление сертификата:**
```bash
# Проверить статус автообновления
systemctl status certbot.timer

# Протестировать обновление (без реального обновления)
certbot renew --dry-run
```

---

### ШАГ 9: Импортировать данные с Railway (если нужно)

**Если у вас есть backup файл с Railway:**

**На локальном компьютере:**
```bash
# Найти backup файл
ls backups/whisperflow_backup_*.sql

# Загрузить на VPS
scp backups/whisperflow_backup_*.sql root@YOUR_VPS_IP:/tmp/backup.sql
```

**На VPS:**
```bash
# Импортировать данные в PostgreSQL
psql -U whisperflow_user -d whisperflow_db -f /tmp/backup.sql

# Проверить импорт
psql -U whisperflow_user -d whisperflow_db -c "\dt"
psql -U whisperflow_user -d whisperflow_db -c "SELECT COUNT(*) FROM users;"
```

---

## ✅ Проверка работоспособности

**На VPS:**
```bash
# Проверить контейнеры
docker compose ps

# Проверить логи приложения
docker compose logs app --tail=50

# Проверить использование ресурсов
docker stats

# Проверить базу данных
psql -U whisperflow_user -d whisperflow_db -c "\dt"

# Проверить работу через домен
curl https://app-toolbox.space
```

**В браузере:**
- ✅ https://app-toolbox.space - главная страница
- ✅ https://app-toolbox.space/login - страница входа
- ✅ Проверить загрузку аудио файла
- ✅ Проверить транскрипцию

---

## 🔧 Полезные команды

```bash
# Перезапустить приложение
cd /opt/whisperflow
docker compose restart

# Остановить приложение
docker compose down

# Запустить приложение
docker compose up -d

# Обновить проект (если изменили код)
git pull origin main
docker compose up -d --build

# Посмотреть логи в реальном времени
docker compose logs -f app

# Подключиться к контейнеру
docker compose exec app bash

# Проверить подключение к PostgreSQL
psql -U whisperflow_user -d whisperflow_db -h localhost
```

---

## 🆘 Решение проблем

### Проблема: Контейнер не запускается

```bash
# Проверить логи
cd /opt/whisperflow
docker compose logs app

# Проверить .env файл
cat .env | grep DATABASE_URL

# Проверить подключение к PostgreSQL
psql -U whisperflow_user -d whisperflow_db -h localhost
```

### Проблема: Ошибка подключения к PostgreSQL

```bash
# Проверить, что PostgreSQL работает
sudo systemctl status postgresql

# Проверить, что база данных существует
sudo -u postgres psql -c "\l" | grep whisperflow_db

# Проверить пользователя
sudo -u postgres psql -c "\du" | grep whisperflow_user

# Попробовать подключиться вручную
psql -U whisperflow_user -d whisperflow_db -h localhost
```

### Проблема: Nginx не работает

```bash
# Проверить конфигурацию
nginx -t

# Проверить статус
systemctl status nginx

# Проверить логи
tail -f /var/log/nginx/whisperflow_error.log
```

### Проблема: SSL не работает

```bash
# Проверить сертификаты
certbot certificates

# Обновить сертификат
certbot renew

# Проверить конфигурацию Nginx
nginx -t
```

### Проблема: Домен не работает

```bash
# Проверить DNS
nslookup app-toolbox.space

# Проверить, что Nginx слушает порт 80
sudo netstat -tlnp | grep :80

# Проверить firewall
sudo ufw status
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

---

## 📝 Чеклист запуска:

- [ ] Подтянуты изменения с GitHub: `git pull origin main`
- [ ] PostgreSQL настроен: `bash scripts/setup_postgres.sh`
- [ ] `.env` файл создан и настроен
- [ ] Docker и Docker Compose установлены
- [ ] Приложение запущено: `docker compose up -d`
- [ ] Приложение отвечает: `curl http://localhost:8000/health`
- [ ] Nginx установлен и настроен
- [ ] DNS записи настроены для домена
- [ ] Домен работает через HTTP: `http://app-toolbox.space`
- [ ] SSL сертификат получен
- [ ] HTTPS работает: `https://app-toolbox.space`
- [ ] Данные импортированы (если нужно)

---

## 🎉 Готово!

После выполнения всех шагов ваш WhisperFlow будет доступен по адресу:
**https://app-toolbox.space**

**Можете начинать использовать приложение!** 🚀
