# ✅ Проверка работы Nginx

## 📋 Проверочные команды:

**На VPS выполните:**

```bash
# 1. Проверить статус Nginx
systemctl status nginx

# 2. Проверить, что конфигурация правильная
cat /etc/nginx/sites-available/whisperflow

# 3. Проверить, что конфигурация активна
ls -la /etc/nginx/sites-enabled/

# 4. Проверить, что Nginx слушает порт 80
netstat -tlnp | grep :80

# 5. Проверить работу через IP (должно работать сразу)
curl http://173.242.53.230

# 6. Проверить работу через домен (если DNS уже обновился)
curl http://app-toolbox.space

# 7. Проверить логи Nginx (если что-то не работает)
tail -f /var/log/nginx/whisperflow_error.log
```

---

## 🎯 Ожидаемый результат:

1. **Status nginx:** должен быть `active (running)`
2. **Конфигурация:** должна содержать `server_name app-toolbox.space www.app-toolbox.space` и `proxy_pass http://localhost:8000`
3. **Порт 80:** должен быть прослушиваем
4. **curl http://173.242.53.230:** должен вернуть HTML страницу входа
5. **curl http://app-toolbox.space:** должен работать после обновления DNS (5-60 минут)

---

## 🔧 Если что-то не работает:

### Проблема: Nginx не работает

```bash
# Перезапустить Nginx
systemctl restart nginx

# Проверить статус
systemctl status nginx

# Проверить конфигурацию
nginx -t

# Посмотреть логи
tail -f /var/log/nginx/error.log
```

### Проблема: Домен не работает (Connection refused)

```bash
# Проверить, что приложение работает
docker compose ps

# Проверить, что приложение отвечает
curl http://localhost:8000/

# Если приложение не работает, запустить
cd /opt/whisperflow
docker compose up -d

# Проверить логи приложения
docker compose logs app --tail=50
```

### Проблема: 502 Bad Gateway

Это означает, что Nginx не может подключиться к приложению на localhost:8000

```bash
# Проверить, что приложение работает
curl http://localhost:8000/

# Если не работает, перезапустить
cd /opt/whisperflow
docker compose restart

# Проверить логи
docker compose logs app
```

---

## ✅ После успешной проверки:

Если все работает, можно получать SSL сертификат!
