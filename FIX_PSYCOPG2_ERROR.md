# 🔧 Исправление ошибки: ModuleNotFoundError: No module named 'psycopg2'

## ❌ Проблема:
```
ModuleNotFoundError: No module named 'psycopg2'
```

## ✅ Решение:
Добавлен `psycopg2-binary` в `requirements.txt` и убран устаревший `version` из `docker-compose.yml`.

## 🚀 Что нужно сделать на VPS:

### ШАГ 1: Подтянуть изменения с GitHub

**На VPS:**
```bash
cd /opt/whisperflow

# Подтянуть последние изменения (включая исправление)
git pull origin main
```

### ШАГ 2: Пересобрать Docker образ

**На VPS:**
```bash
cd /opt/whisperflow

# Остановить контейнер (если запущен)
docker compose down

# Пересобрать образ с новыми зависимостями
docker compose build --no-cache

# Это может занять 5-10 минут, так как установится psycopg2-binary
```

### ШАГ 3: Запустить контейнер заново

**На VPS:**
```bash
# Запустить контейнер
docker compose up -d

# Проверить статус
docker compose ps

# Посмотреть логи (первые 50 строк)
docker compose logs app --tail=50
```

**Ожидаемый результат:**
- ✅ Контейнер `app` должен быть в статусе `Up`
- ✅ В логах должно быть: "Using PostgreSQL database from DATABASE_URL"
- ✅ НЕТ ошибки "ModuleNotFoundError: No module named 'psycopg2'"

### ШАГ 4: Проверить работу приложения

**На VPS:**
```bash
# Подождать 10-15 секунд для полного запуска
sleep 15

# Проверить health endpoint
curl http://localhost:8000/health

# Или проверить главную страницу
curl http://localhost:8000/
```

**Если приложение отвечает** - значит все работает! ✅

---

## 🔍 Если что-то не работает:

### Проблема: Все еще ошибка psycopg2

**Решение:**
```bash
# Убедиться, что изменения подтянуты
cd /opt/whisperflow
git pull origin main
cat requirements.txt | grep psycopg2-binary

# Пересобрать образ БЕЗ кэша
docker compose build --no-cache

# Перезапустить
docker compose down
docker compose up -d

# Проверить логи
docker compose logs app --tail=50
```

### Проблема: Контейнер не запускается

**Решение:**
```bash
# Посмотреть детальные логи
docker compose logs app

# Проверить .env файл
cat .env | grep DATABASE_URL

# Проверить подключение к PostgreSQL
psql -U whisperflow_user -d whisperflow_db -c "SELECT 1;"
```

---

## ✅ После успешного запуска:

1. ✅ Приложение должно работать на `http://localhost:8000`
2. ✅ Health endpoint должен отвечать: `curl http://localhost:8000/health`
3. ✅ Можно переходить к настройке Nginx и домена

---

**Выполните ШАГИ 1-4 выше, и приложение должно заработать!** 🚀
